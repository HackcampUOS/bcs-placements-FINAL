<?php require('template/header.phtml'); ?>

    <main class="page-content">

    <div class="container-fluid">

        <!-- Success/Error Messages -->
        <?php if (!empty($view->message)): ?>
            <div class="row my-4">
                <div class="col-12">
                    <div class="alert alert-success alert-dismissible fade show rounded-4 shadow-lg" role="alert">
                        <?= $view->message ?>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                </div>
            </div>
        <?php endif; ?>

        <?php if (!empty($view->errorMessage)): ?>
            <div class="row my-4">
                <div class="col-12">
                    <div class="alert alert-danger alert-dismissible fade show rounded-4 shadow-lg" role="alert">
                        <?= htmlspecialchars($view->errorMessage) ?>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                </div>
            </div>
        <?php endif; ?>

        <!-- Back Button -->
        <div class="row px-4 mt-3 mb-3">
            <div class="col-12">
                <a href="placements.php" class="btn btn-outline-light rounded-3 shadow-sm pop-obj">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left" viewBox="0 0 16 16">
                        <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8"/>
                    </svg> Back to All Opportunities
                </a>
            </div>
        </div>

        <!-- Page Heading (Job Title) -->
        <div class="row px-4">
            <div id="title" class="col-12 mb-0">
                <h2 class="pg-heading text-white fw-bold px-2 py-0 paw-bg">
                    <?= htmlspecialchars($view->placement->getTitle()) ?>
                </h2>
            </div>
        </div>

        <br>

        <!-- Main Content -->
        <div class="row px-4">
            <!-- Left Column - Company Info & Key Details -->
            <div class="col-lg-4 mb-4">
                <!-- Company Card -->
                <div class="card shadow-lg rounded-4 mb-4">
                    <div class="card-header text-white shadow" style="background-color: #212529; border-top-left-radius: 15px; border-top-right-radius: 15px">
                        <h4 class="mb-0"><strong>🏢 Company</strong></h4>
                    </div>
                    <div class="card-body paw-bg text-center">
                        <!-- Company Logo -->
                        <?php if ($view->placement->getCompanyLogo()): ?>
                            <img src="<?= htmlspecialchars($view->placement->getCompanyLogo()) ?>"
                                 alt="<?= htmlspecialchars($view->placement->getCompanyName()) ?>"
                                 class="img-fluid rounded-3 mb-3"
                                 style="max-width: 200px; max-height: 100px; object-fit: contain;">
                        <?php else: ?>
                            <div class="bg-white rounded-3 d-flex align-items-center justify-content-center text-white mb-3 mx-auto"
                                 style="width: 200px; height: 100px;">
                                <span class="display-3">🏢</span>
                            </div>
                        <?php endif; ?>

                        <h4 class="fw-bold"><?= htmlspecialchars($view->placement->getCompanyName()) ?></h4>

                        <?php if (!empty($view->placement->getWebsite())): ?>
                            <a href="<?= htmlspecialchars($view->placement->getWebsite()) ?>"
                               target="_blank"
                               class="btn btn-outline-primary btn-sm rounded-3 mt-2">
                                Visit Website <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-box-arrow-up-right" viewBox="0 0 16 16">
                                    <path fill-rule="evenodd" d="M8.636 3.5a.5.5 0 0 0-.5-.5H1.5A1.5 1.5 0 0 0 0 4.5v10A1.5 1.5 0 0 0 1.5 16h10a1.5 1.5 0 0 0 1.5-1.5V7.864a.5.5 0 0 0-1 0V14.5a.5.5 0 0 1-.5.5h-10a.5.5 0 0 1-.5-.5v-10a.5.5 0 0 1 .5-.5h6.636a.5.5 0 0 0 .5-.5"/>
                                    <path fill-rule="evenodd" d="M16 .5a.5.5 0 0 0-.5-.5h-5a.5.5 0 0 0 0 1h3.793L6.146 9.146a.5.5 0 1 0 .708.708L15 1.707V5.5a.5.5 0 0 0 1 0z"/>
                                </svg>
                            </a>
                        <?php endif; ?>
                    </div>
                </div>

                <!-- Key Details Card -->
                <div class="card shadow-lg rounded-4 mb-4">
                    <div class="card-header text-white shadow" style="background-color: #212529; border-top-left-radius: 15px; border-top-right-radius: 15px">
                        <h4 class="mb-0"><strong>📋 Key Details</strong></h4>
                    </div>
                    <div class="card-body paw-bg">
                        <p class="mb-2">
                            <strong>📍 Location:</strong><br>
                            <?= htmlspecialchars($view->placement->getLocation()) ?>
                        </p>
                        <hr>
                        <p class="mb-2">
                            <strong>💰 Salary:</strong><br>
                            <span class="text-success"><?= $view->placement->getSalaryRange() ?></span>
                        </p>
                        <hr>
                        <p class="mb-2">
                            <strong>📅 Start Date - End Date:</strong><br>
                            <?= $view->placement->getFormattedStartDate() ?> - <?= $view->placement->getFormattedEndDate() ?>
                        </p>
                        <hr>
                        <p class="mb-2">
                            <strong>⏱️ Duration:</strong><br>
                            <?= $view->placement->getDurationMonths() ?> months
                        </p>
                        <hr>
                        <p class="mb-2">
                            <strong>⏰ Application Deadline:</strong><br>
                            <span class="<?= $view->placement->isDeadlineSoon() ? 'text-danger fw-bold' : '' ?>">
                            <?= $view->placement->getFormattedDeadline() ?>
                        </span>
                            <?php if ($view->placement->getDaysUntilDeadline() !== null && $view->placement->getDaysUntilDeadline() >= 0): ?>
                                <br><small class="text-muted">(<?= $view->placement->getDaysUntilDeadline() ?> days remaining)</small>
                            <?php endif; ?>
                        </p>
                        <?php if ($view->user->isLoggedIn() && $view->user->getUserType() === 'student'): ?>
                        <?php $daysLeft = $view->placement->getDaysUntilDeadline(); ?>
                            <?php if ($daysLeft !== null && $daysLeft <= 7 && $daysLeft > 0): ?>
                                <div class="alert alert-warning mt-3 rounded-3" role="alert">
                                    <strong>⚠️ Deadline approaching!</strong><br>
                                    Apply soon to not miss out!
                                </div>
                        <?php endif; ?>
                            <?php if ($daysLeft !== null && $daysLeft == 0): ?>
                                <div class="alert alert-warning mt-3 rounded-3" role="alert">
                                    <strong>⚠️ Deadline Today!</strong><br>
                                    Apply now to not miss out!
                                </div>
                            <?php endif; ?>
                        <?php endif; ?>
                    </div>
                </div>

                <!-- Action Buttons (For Students) -->
                <?php if ($view->user->isLoggedIn() && $view->user->getUserType() === 'student'): ?>
                    <div class="card shadow-lg rounded-4">
                        <div class="card-body paw-bg d-grid gap-2">
                            <!-- Apply Button -->
                            <form method="POST" target="_blank action="placement_details.php?id=<?= $view->placement->getPlacementId() ?>">
                                <button type="submit" name="apply_btn" class="btn btn-success btn-lg w-100 rounded-3 shadow pop-obj">
                                    ✅ Apply Now
                                </button>
                            </form>

                            <!-- Shortlist Button -->
                            <form method="POST" action="placement_details.php?id=<?= $view->placement->getPlacementId() ?>">
                                <button type="submit" name="toggle_shortlist" class="btn btn-<?= $view->isShortlisted ? 'danger' : 'outline-primary' ?> btn-lg w-100 rounded-3 shadow pop-obj">
                                    <?= $view->isShortlisted ? '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-bookmark-dash" viewBox="0 0 16 16">
                                        <path fill-rule="evenodd" d="M5.5 6.5A.5.5 0 0 1 6 6h4a.5.5 0 0 1 0 1H6a.5.5 0 0 1-.5-.5"/>
                                        <path d="M2 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v13.5a.5.5 0 0 1-.777.416L8 13.101l-5.223 2.815A.5.5 0 0 1 2 15.5zm2-1a1 1 0 0 0-1 1v12.566l4.723-2.482a.5.5 0 0 1 .554 0L13 14.566V2a1 1 0 0 0-1-1z"/>
                                        </svg> Remove from Shortlist' : '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-bookmark-plus" viewBox="0 0 16 16">
                                        <path d="M2 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v13.5a.5.5 0 0 1-.777.416L8 13.101l-5.223 2.815A.5.5 0 0 1 2 15.5zm2-1a1 1 0 0 0-1 1v12.566l4.723-2.482a.5.5 0 0 1 .554 0L13 14.566V2a1 1 0 0 0-1-1z"/>
                                        <path d="M8 4a.5.5 0 0 1 .5.5V6H10a.5.5 0 0 1 0 1H8.5v1.5a.5.5 0 0 1-1 0V7H6a.5.5 0 0 1 0-1h1.5V4.5A.5.5 0 0 1 8 4"/>
                                        </svg> Shortlist' ?>
                                </button>
                            </form>
                        </div>
                    </div>
                <?php elseif (!$view->user->isLoggedIn()): ?>
                    <div class="alert alert-info rounded-4 shadow-lg text-center" role="alert">
                        <strong>🔐 Sign in required</strong><br>
                        <a href="login.php" class="alert-link">Login or register</a> to apply for this placement.
                    </div>
                <?php endif; ?>

                <?php if ($view->user->isLoggedIn() && $view->user->getUserType() === 'employer'): ?>
                    <div class="alert alert-info rounded-4 shadow-lg text-center" role="alert">
                        <strong>Only registered students can apply for placements</strong>
                    </div>
                <?php endif; ?>

            </div>

            <!-- Right Column - Description & Requirements -->
            <div class="col-lg-8 mb-4">
                <!-- Description Card -->
                <div class="card shadow-lg rounded-4 mb-4">
                    <div class="card-header text-white shadow" style="background-color: #212529; border-top-left-radius: 15px; border-top-right-radius: 15px">
                        <h4 class="mb-0"><strong>📝 Job Description</strong></h4>
                    </div>
                    <div class="card-body paw-bg">
                        <p style="white-space: pre-wrap;"><?= htmlspecialchars($view->placement->getDescription()) ?></p>
                    </div>
                </div>

                <!-- Required Skills Card -->
                <?php if ($view->placement->hasSkills()): ?>
                    <div class="card shadow-lg rounded-4 mb-4">
                        <div class="card-header text-white shadow" style="background-color: #212529; border-top-left-radius: 15px; border-top-right-radius: 15px">
                            <div class="d-flex justify-content-between align-items-center">
                                <h4 class="mb-0"><strong>🎯 Required Skills (SFIA)</strong></h4>

                                <?php if (isset($view->matchPercentage) && $view->matchPercentage !== null): ?>
                                    <?php
                                    $match = $view->matchPercentage;
                                    $matchClass = ($match === 100) ? 'bg-success' : 'bg-secondary';
                                    ?>
                                    <span class="badge <?= $matchClass ?>">Match: <?= $match ?>%</span>
                                <?php endif; ?>
                            </div>
                        </div>
                        <div class="card-body paw-bg">
                            <div class="row">
                                <?php foreach ($view->placement->getSkills() as $skill): ?>
                                    <div class="col-md-6 mb-3">
                                        <div class="d-flex align-items-center">
                                        <span class="badge bg-primary me-2" style="min-width: 80px;">
                                            Level <?= $skill['required_proficiency'] ?>
                                        </span>
                                            <strong><?= htmlspecialchars($skill['skill_name']) ?></strong>
                                        </div>
                                        <small class="text-muted ms-5 ps-5">
                                            Skill Code: <?= htmlspecialchars($skill['skill_code']) ?>
                                        </small>
                                    </div>
                                <?php endforeach; ?>
                            </div>
                            <hr>
                            <p class="mb-0 text-muted">
                                <small>
                                    <strong>Note:</strong> Skills are based on the SFIA (Skills Framework for the Information Age) framework.
                                    Level 1-7 indicates proficiency from awareness to expert level.
                                </small>
                            </p>
                        </div>
                    </div>
                <?php endif; ?>

                <!-- Company About (if available) -->
                <?php if (!empty($view->placement->getCompanyDescription())): ?>
                    <div class="card shadow-lg rounded-4">
                        <div class="card-header text-white shadow" style="background-color: #212529; border-top-left-radius: 15px; border-top-right-radius: 15px">
                            <h4 class="mb-0"><strong>🏢 About <?= htmlspecialchars($view->placement->getCompanyName()) ?></strong></h4>
                        </div>
                        <div class="card-body paw-bg">
                            <p style="white-space: pre-wrap;"><?= htmlspecialchars($view->placement->getCompanyDescription()) ?></p>
                        </div>
                    </div>
                <?php endif; ?>
            </div>
        </div>

    </div>

    </main>

<?php require('template/footer.phtml'); ?>